cc_library(
    name = "btstack_config",
    hdrs = ["btstack_config.h"],
    defines = [
        "ENABLE_CLASSIC=1",
        "ENABLE_BLE=1",
    ],
    includes = ["."],
)

cc_library(
    name = "lwip_config",
    hdrs = ["lwipopts.h"],
    includes = ["."],
)